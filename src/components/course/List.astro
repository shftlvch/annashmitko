---
import type { HTMLAttributes } from "astro/types";

interface Props extends HTMLAttributes<"ul"> {
  items: string[];
}

const { items, class: className, ...props } = Astro.props;
---

<ul
  class:list={[
    "list-disc pl-5 text-xl font-normal font-sans leading-loose",
    className,
  ]}
  {...props}
>
  {items.map((item) => <li class="mb-2 leading-normal">{item}</li>)}
</ul>
